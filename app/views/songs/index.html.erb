<h1>Songs</h1>
  <hr>
<% @songs.each do |song| %>
<%= song.title %> - <%= song.artist %>
<%= link_to "View Song", song_path(song) %>
<%= link_to "Edit Song", edit_song_path(song) %>
<br>
<hr>
<%= link_to "New Song", new_song_path %>
<hr>
<% end %>
